// Generated by tools/gen_rest_api.py. Do not edit.
import Foundation

/// Generated from the reviewed REST response for GET /api/audio/voice-live/status.
public struct AudioVoiceLiveStatusResponse: Codable, Sendable, Hashable {
    public let available: Bool
    public let mode: String
    public let model: String
    public let ok: Bool
    public let reason: String?
    public let voice: String

    public init(available: Bool, mode: String, model: String, ok: Bool, reason: String?, voice: String) {
        self.available = available
        self.mode = mode
        self.model = model
        self.ok = ok
        self.reason = reason
        self.voice = voice
    }

    private enum CodingKeys: String, CodingKey {
        case available = "available"
        case mode = "mode"
        case model = "model"
        case ok = "ok"
        case reason = "reason"
        case voice = "voice"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["available", "mode", "model", "ok", "reason", "voice"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        available = try container.decode(Bool.self, forKey: .available)
        mode = try container.decode(String.self, forKey: .mode)
        model = try container.decode(String.self, forKey: .model)
        ok = try container.decode(Bool.self, forKey: .ok)
        guard container.contains(.reason) else {
            throw DecodingError.keyNotFound(CodingKeys.reason, .init(codingPath: decoder.codingPath, debugDescription: "Missing required REST field"))
        }
        reason = try container.decodeIfPresent(String.self, forKey: .reason)
        voice = try container.decode(String.self, forKey: .voice)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(available, forKey: .available)
        try container.encode(mode, forKey: .mode)
        try container.encode(model, forKey: .model)
        try container.encode(ok, forKey: .ok)
        try container.encode(reason, forKey: .reason)
        try container.encode(voice, forKey: .voice)
    }
}

/// Generated from the reviewed REST response for GET /api/auth/me.
public struct AuthMeResponse: Codable, Sendable, Hashable {
    public let displayName: String
    public let email: String
    public let expiresAt: Int
    public let orgId: String
    public let provider: String
    public let userId: String

    public init(displayName: String, email: String, expiresAt: Int, orgId: String, provider: String, userId: String) {
        self.displayName = displayName
        self.email = email
        self.expiresAt = expiresAt
        self.orgId = orgId
        self.provider = provider
        self.userId = userId
    }

    private enum CodingKeys: String, CodingKey {
        case displayName = "display_name"
        case email = "email"
        case expiresAt = "expires_at"
        case orgId = "org_id"
        case provider = "provider"
        case userId = "user_id"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["display_name", "email", "expires_at", "org_id", "provider", "user_id"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        displayName = try container.decode(String.self, forKey: .displayName)
        email = try container.decode(String.self, forKey: .email)
        expiresAt = try container.decode(Int.self, forKey: .expiresAt)
        orgId = try container.decode(String.self, forKey: .orgId)
        provider = try container.decode(String.self, forKey: .provider)
        userId = try container.decode(String.self, forKey: .userId)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(displayName, forKey: .displayName)
        try container.encode(email, forKey: .email)
        try container.encode(expiresAt, forKey: .expiresAt)
        try container.encode(orgId, forKey: .orgId)
        try container.encode(provider, forKey: .provider)
        try container.encode(userId, forKey: .userId)
    }
}

/// Generated from the reviewed REST response for POST /api/auth/ws-ticket.
public struct AuthWsTicketResponse: Codable, Sendable, Hashable {
    public let ticket: String
    public let ttlSeconds: Int

    public init(ticket: String, ttlSeconds: Int) {
        self.ticket = ticket
        self.ttlSeconds = ttlSeconds
    }

    private enum CodingKeys: String, CodingKey {
        case ticket = "ticket"
        case ttlSeconds = "ttl_seconds"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["ticket", "ttl_seconds"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        ticket = try container.decode(String.self, forKey: .ticket)
        ttlSeconds = try container.decode(Int.self, forKey: .ttlSeconds)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(ticket, forKey: .ticket)
        try container.encode(ttlSeconds, forKey: .ttlSeconds)
    }
}

/// Generated from the reviewed REST response for GET /api/profiles/active.
public struct ProfilesActiveResponse: Codable, Sendable, Hashable {
    public let active: String
    public let current: String

    public init(active: String, current: String) {
        self.active = active
        self.current = current
    }

    private enum CodingKeys: String, CodingKey {
        case active = "active"
        case current = "current"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["active", "current"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        active = try container.decode(String.self, forKey: .active)
        current = try container.decode(String.self, forKey: .current)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(active, forKey: .active)
        try container.encode(current, forKey: .current)
    }
}

/// Generated from the OpenAPI request for POST /api/profiles/active.
public struct ProfilesSetActiveRequest: Codable, Sendable, Hashable {
    public let name: String

    public init(name: String) {
        self.name = name
    }

    private enum CodingKeys: String, CodingKey {
        case name = "name"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["name"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST request field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        name = try container.decode(String.self, forKey: .name)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
    }
}

/// Generated from the reviewed REST response for POST /api/profiles/active.
public struct ProfilesSetActiveResponse: Codable, Sendable, Hashable {
    public let active: String
    public let ok: Bool

    public init(active: String, ok: Bool) {
        self.active = active
        self.ok = ok
    }

    private enum CodingKeys: String, CodingKey {
        case active = "active"
        case ok = "ok"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["active", "ok"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        active = try container.decode(String.self, forKey: .active)
        ok = try container.decode(Bool.self, forKey: .ok)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(active, forKey: .active)
        try container.encode(ok, forKey: .ok)
    }
}

/// Generated from the reviewed REST response for GET /api/sessions/empty/count.
public struct SessionsEmptyCountResponse: Codable, Sendable, Hashable {
    public let count: Int

    public init(count: Int) {
        self.count = count
    }

    private enum CodingKeys: String, CodingKey {
        case count = "count"
    }

    public init(from decoder: Decoder) throws {
        let raw = try decoder.container(keyedBy: DynamicCodingKey.self)
        let allowed: Set<String> = ["count"]
        guard raw.allKeys.allSatisfy({ allowed.contains($0.stringValue) }) else {
            throw DecodingError.dataCorrupted(.init(codingPath: decoder.codingPath, debugDescription: "Unexpected REST response field"))
        }
        let container = try decoder.container(keyedBy: CodingKeys.self)
        count = try container.decode(Int.self, forKey: .count)
    }

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(count, forKey: .count)
    }
}
