/// Entry point for the generated Hermes gateway and dashboard clients.
public enum HermesAPI {
    /// The upstream Hermes release used to generate this package.
    public static let hermesRelease = "v2026.9.21"
}
